import { HotelsFilterPipe } from './hotels-filter.pipe';

describe('HotelsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HotelsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
